import advisories from '../src/advisories.js';

export const NUM_OF_ADVISORIES = Object.keys(advisories).length;
export const PACKAGE_REPORT_SELECTOR = 'overlay-package-report';
export const TOOLTIP_SOURCES_SELECTOR = '.overlay-indicator__tooltip__sources > .overlay-indicator__tooltip__source';
